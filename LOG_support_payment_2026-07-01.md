# Лог работ: «Оплата через поддержку» + инфраструктура развёртывания
**Дата:** 2026-07-01
**Исполнитель:** Claude (Opus 4.8)
**Сервер панели:** 109.107.155.40 · **Нода:** 138.124.251.48

---

## 1. Краткое содержание

Реализована фича **«оплата через поддержку»** (пополнение баланса и покупка подписки
через заявку-тикет с подтверждением админом), исправлен роутинг бекап-уведомлений,
настроены TLS-уведомления, создан форк бота и два скрипта развёртывания (стабильный
и безопасно-обновляемый).

---

## 2. Репозитории и версии (точки отката по коммитам)

| Репозиторий | Назначение | Ключевые коммиты/теги |
|-------------|-----------|------------------------|
| [FnoUp/remnawave-bedolaga-telegram-bot](https://github.com/FnoUp/remnawave-bedolaga-telegram-bot) | **Форк бота** с фичей | `0c9b7b21` (баланс+backup), `15572140` (подписка+UX), `8616f543` (фикс кнопок в группе), тег `stable-support-payment-v1` → `8616f543` |
| [FnoUp/bedolaga_installer](https://github.com/FnoUp/bedolaga_installer) | Установщик и скрипты | `deploy_stable.sh`, `update_safe.sh`, этот лог |
| BEDOLAGA-DEV/...（upstream) | Оригинал автора | подключён как `upstream` для merge |

**Сервер `/opt/bedolaga`:** `origin` переключён на форк, зафиксирован на `15572140`.

---

## 3. Что именно изменено (поэтапно)

### Этап A. Настройки (.env на сервере) — обратимо
- `ADMIN_REPORTS_SEND_TIME`: `10:00` → `03:00` (= 05:00 по Перми)
- `TELEGRAM_STARS_ENABLED`: `false` → `true` (дефолтный метод)
- TZ оставлен `Europe/Moscow`
- **Бэкап:** `/opt/bedolaga/.env.bak.<дата>`

### Этап B. Фикс роутинга бекапов (код, в форке)
**Файл:** `app/services/backup_service.py`, метод `_send_backup_notification`
- Было: текст «СИСТЕМА БЕКАПОВ» → категория `INFRASTRUCTURE` (топик 4)
- Стало: текст → `BACKUP_SEND_TOPIC_ID` (топик 13); ошибки бекапа дублируются в категорию `ERRORS` (топик 4)

### Этап C. Фича «оплата через поддержку» (код, в форке)
**Новые файлы:**
- `app/database/models.py` → класс `SupportPaymentRequest` (добавлен в конец)
- `migrations/alembic/versions/0095_add_support_payment_requests.py` → таблица `support_payment_requests`
- `app/database/crud/support_payment.py` → CRUD (create / get / set_status)

**Изменён:** `app/handlers/balance/main.py`
- `request_support_topup` переделан: статичный текст → рабочий поток
  - **Баланс:** запрос суммы → тикет → заявка → уведомление админам с кнопками
  - **Подписка:** если есть сохранённая корзина (`missing_amount`) — сумма берётся
    автоматически, без запроса
- Уведомление админам (топик 11): кнопки **💬 Ответить · ✅ Подтвердить · ❌ Отклонить**
- `confirm_support_payment`: зачисление баланса (`add_user_balance`, с блокировкой строки),
  закрытие тикета, уведомление юзеру; для подписки — кнопка «Завершить покупку»
- `reject_support_payment`: отклонение, закрытие тикета, уведомление юзеру
- Кнопка «Назад» в подтверждении заявки → `back_to_menu`

### Этап D. Развёртывание
- Сервер переведён с upstream на форк (`git reset --hard 15572140`), пересборка образа
- Миграция `0095` применилась автоматически при старте (таблица создана)
- Установщик `install_bedolaga.sh`: `REPO_URL` → форк

### Этап E. Скрипты (в installer-репо)
- `deploy_stable.sh` — деплой зафиксированной версии из форка + health-check + откат
- `update_safe.sh` — безопасное обновление (merge upstream → проверка → авто-откат)

### Этап H. Кнопки оплаты не пропадают после ответа (код, в форке) — коммит `4ba7a528`
**Проблема:** после «Ответить» сообщение заявки редактировалось → кнопки
Подтвердить/Отклонить исчезали.
- `get_pending_request_by_ticket` — поиск активной заявки по тикету
- уведомление о сообщении юзера в платёжном тикете → добавлены кнопки оплаты
- подтверждение/отмена ответа админа в платёжном тикете → добавлены кнопки оплаты
- Тег `stable-support-payment-v1` → `4ba7a528`

### Этап G. Предпросмотр ответа в тикете (код, в форке) — коммит `1834729d`
**Файл:** `app/handlers/admin/tickets.py`
- Раньше: админ вводил текст → ответ сразу уходил пользователю
- Теперь: ввод текста → **предпросмотр** с кнопками «✅ Отправить» / «❌ Отменить»;
  ответ уходит только после подтверждения. Черновик (текст+медиа) в FSM,
  повторный ввод обновляет черновик
- Новые callback'и: `admin_reply_send`, `admin_reply_discard`
- Тег `stable-support-payment-v1` → `1834729d`

### Этап F. Фикс: кнопки в групповом чате не работали (код, в форке) — коммит `8616f543`
**Причина:** мидлварь `ChatTypeFilterMiddleware` глушила ВСЕ события из непривата
(групп) до логирования — поэтому нажатия кнопок заявок в топике 11 не доходили
до обработчиков.
- `app/middlewares/chat_type_filter.py`: добавлено исключение — события из
  настроенных админ-чатов (`ADMIN_NOTIFICATIONS/REPORTS/BACKUP/LOG_CHAT_ID`)
  больше не глушатся
- `app/handlers/common.py` (`handle_unknown_message`): в группах бот молчит
  (чтобы разрешение чата не создавало шум на обычный текст админов)
- Проверено: `_admin_group_chat_ids()` → `{-1003978283456}`, бот стартует без ошибок

---

## 4. Бэкапы (созданы на сервере)

| Что | Путь |
|-----|------|
| Конфиг до правок | `/opt/bedolaga/.env.bak.<дата>` |
| Код бота (app+migrations) | `/opt/bedolaga_app_backup_<дата>.tar.gz` |
| Оригинал backup_service | `/opt/bedolaga/app/services/backup_service.py.bak.<дата>` |
| Дамп БД (создаётся скриптами) | `/opt/bedolaga/db_backup_<дата>.sql.gz` |
| Образ перед обновлением | `docker image bedolaga-bot:rollback-<дата>` |

---

## 5. ПЛАН ОТКАТА (по слоям)

### 5.1 Откатить только .env (отчёты/Stars)
```bash
cd /opt/bedolaga
cp .env.bak.<дата> .env
docker compose restart bot
```

### 5.2 Откатить фичу (вернуть оригинал автора)
```bash
cd /opt/bedolaga
git remote set-url origin https://github.com/BEDOLAGA-DEV/remnawave-bedolaga-telegram-bot.git
git fetch origin
git reset --hard origin/main
docker compose build bot && docker compose up -d bot
```
> Таблица `support_payment_requests` останется в БД — она безвредна. Удалить (опционально):
> `docker exec remnawave_bot_db psql -U remnawave_user -d remnawave_bot -c 'DROP TABLE IF EXISTS support_payment_requests;'`

### 5.3 Полный откат из бэкапа кода
```bash
cd /opt && tar xzf bedolaga_app_backup_<дата>.tar.gz -C /opt/bedolaga
cd /opt/bedolaga && docker compose build bot && docker compose up -d bot
```

### 5.4 Восстановить БД из дампа
```bash
gunzip -c /opt/bedolaga/db_backup_<дата>.sql.gz | docker exec -i remnawave_bot_db psql -U remnawave_user -d remnawave_bot
```

### 5.5 Откат конкретного коммита фичи
```bash
cd /opt/bedolaga && git reset --hard 0c9b7b21   # до подписок (только баланс)
# или git reset --hard <upstream> для полного отката
docker compose build bot && docker compose up -d bot
```

---

## 6. Два сценария эксплуатации (как просили)

### Скрипт 1 — `deploy_stable.sh` (стабильно, со своего репо)
- Разворачивает бот, **зафиксированный** на проверенной версии (тег `stable-support-payment-v1`)
- Версия не «уедет» сама; не зависит от доступности/изменений upstream
- Делает бэкап (.env + дамп БД), health-check, при сбое — авто-откат
- Сменить версию: `PINNED_REF=<тег|коммит> bash deploy_stable.sh`

### Скрипт 2 — `update_safe.sh` (устойчиво к любым апдейтам)
- `--check` — посмотреть, есть ли апдейты upstream (без изменений)
- Без флага — влить upstream через **merge**:
  - **конфликт** → обновление отменяется, прод продолжает работать (ничего не ломается)
  - **чисто** → пересборка + health-check; при сбое — **авто-откат** на рабочую версию
- Бэкап БД/образа/.env перед каждым обновлением, уведомления в Telegram

> Стратегия «не сломается в будущем»: фича живёт в форке, апдейты автора вливаются
> git-merge'ем. Git сам сливает непересекающиеся изменения; при пересечении — безопасно
> останавливается, а не ломает прод. Фича вынесена в отдельные файлы (CRUD, миграция,
> модель в конце файла) → минимальная площадь конфликтов.

---

## 7. Продуманные риски и защита

| Риск | Решение |
|------|---------|
| Двойное зачисление (двойной клик «Подтвердить») | Проверка `status == 'pending'` перед обработкой |
| Гонки баланса | `add_user_balance` блокирует строку (`SELECT … FOR UPDATE`) |
| Подтверждение не-админом | `settings.is_admin(callback.from_user.id)` в обоих хендлерах |
| Поломка при обновлении бота | `update_safe.sh`: merge + health-check + авто-откат |
| Затирание фичи при `git pull` | Фича в форке; сервер тянет форк, а не upstream |
| Потеря данных при апдейте | Дамп БД + бэкап .env + сохранение образа перед каждым обновлением |
| Сломанная миграция | Идемпотентна (проверка существования таблицы), авто-применяется при старте |
| Секреты в логах | Токен читается из .env, в чат не выводится |

---

## 8. Тестирование (выполнено)

- ✅ Миграция `0095` применилась, таблица `support_payment_requests` создана
- ✅ Бот стартует без ошибок после деплоя фичи
- ✅ Создание заявки (баланс): тикет + заявка + уведомление в топик 11 с 3 кнопками
- ✅ Записи заявок в БД корректны (`pending`)
- ✅ Бекап-уведомление уходит в топик 13 (проверено)
- ✅ TLS-хук: панель + нода France (проверено)
- ✅ `update_safe.sh --check` корректно определяет отсутствие апдейтов
- ✅ Синтаксис всех .py и .sh файлов проверен

**Проверить вручную в Telegram (по кнопкам):**
- Нажать ✅ на заявке в топике 11 → баланс зачисляется, тикет закрывается, юзеру приходит уведомление
- Покупка подписки при нехватке баланса → «через поддержку» → заявка без запроса суммы

---

## 9. Известные ограничения / на будущее

- Подписка через поддержку: после подтверждения баланс пополняется и юзер
  завершает покупку кнопкой «Завершить покупку» (1 тап). Полностью автоматическая
  активация подписки без действия юзера — возможна, но намеренно не сделана ради
  надёжности (не реплеим сложную checkout-логику вне контекста пользователя).
- Локализация: ключи добавлены с дефолтами (RU/EN видят корректный текст; ua/zh/fa
  используют дефолт-фолбэк). При желании — добавить переводы в locale-файлы.
- Поддержка форка: периодически запускать `update_safe.sh` для вливания апдейтов автора.
